
User issue: Telegram-side Dori says "no emails today" because Gmail sync only runs on-demand (when user opens the Email Hub in the web app). Telegram has no fresh data to query.

Let me verify by checking the gmail-sync function and how it's currently triggered.
